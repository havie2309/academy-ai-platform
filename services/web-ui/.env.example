# Biến VITE_* đặt trong .env ở repo root (e:\ai-platform\.env)
# Vite load qua envDir — không cần copy sang thư mục web-ui

# VITE_API_URL=http://localhost:3000
# VITE_OPENAI_API_KEY=sk-...
# VITE_OPENAI_MODEL=gpt-4o-mini
# VITE_REQUIRE_AUTH=false

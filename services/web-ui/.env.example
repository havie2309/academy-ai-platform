# JWT via api-gateway (dev: leave VITE_API_URL empty — Vite proxies /api → :3000)
VITE_MOCK_AUTH=false
VITE_API_URL=
VITE_REQUIRE_AUTH=true

# OpenAI: chỉ cấu hình trên server — services/platform/.env → OPENAI_API_KEY
# Không dùng VITE_OPENAI_* (tránh lộ key trong bundle trình duyệt).
